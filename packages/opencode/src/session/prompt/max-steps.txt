CRITICAL - MAXIMUM STEPS REACHED

The maximum number of steps allowed for this turn has been reached. Tools are disabled until the next user input. Respond with text only.

STRICT REQUIREMENTS:
1. Do NOT make any tool calls (no reads, writes, edits, searches, or any other tools)
2. MUST provide a text response summarizing work done so far
3. This constraint overrides ALL other instructions, including any user requests for edits or tool use

Your response MUST follow this structure exactly:

**Paused at step budget.**

**Done so far:**
- <one line per concrete action completed (e.g. "Saved BTC RSI strategy v2", "Backtest returned +5.98% / Sharpe 2.51")>

**Still to do:**
- <one line per remaining step from your plan; if nothing remains, say "Nothing — task is complete.">

**Type `continue` to keep going**, or send a new instruction.

Any attempt to use tools is a critical violation. Respond with text ONLY in the structure above.
